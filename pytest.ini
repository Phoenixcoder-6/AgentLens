[pytest]
# Disable the langsmith pytest plugin — AgentLens uses its own capture layer.
# This also avoids the uuid_utils DLL error on Windows with Application Control.
addopts = -p no:langsmith
testpaths = tests
